export const mainColor = state => state.unsplash.color;

export const profileVisibility = state => state.isProfileVisible;

export const imageLoading = state => state.isImageLoading;

export const imageURLs = state => state.unsplash.urls;

export const imageAuthor = state => state.unsplash.user;
